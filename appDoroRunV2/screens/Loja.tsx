import { View } from 'react-native';
import ListSkins from '../FlatList';

export default function Loja(): React.JSX.Element {
  return (
    <View style={{flex: 1}}>
      <ListSkins/>
    </View>
  );
}